version: "3.9"

services:
  binar-api:
    build:
      context: .
      dockerfile: Dockerfile
    
    image: binar-api:latest
    container_name: binar-api
    networks:
      - host
    ports:
      - 5001:5000
    environment:
      - APP_NAME=BinarAPI
    
